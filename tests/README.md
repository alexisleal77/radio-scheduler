# tests

Functional tests that check schedulers and scenario generators against expected outputs for fixed, deterministic scenarios.

This is about correctness, not performance — a separate concern from `src/benchmark/`.

Status: not yet implemented. See [`docs/architecture.md`](../docs/architecture.md).
